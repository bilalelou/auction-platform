# Frontend (Next.js)

This is the Next.js UI for the auction (مزاد).

Run from repo root instructions in `README.md`.

Quick start:

```powershell
cd frontend
copy .env.local.example .env.local
npm install
npm run dev
```